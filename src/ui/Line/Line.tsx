import { Box } from "@mui/material";
import React from "react";

const Line = () => {
  return (
    <Box
      sx={{
        background: "#2640E3",
        width: "60px",
        height: "5px",
        borderRadius: "25px",
      }}
    />
  );
};

export default Line;
